/**
 * An abstraction level over binary streams
 *
 * This class provides a unified access to binary streams and can be used for 
 * reading binary files / binary data from RAM / whatever.
 */
class BStream
{
	private:

	public:
		bool read()
			
};
